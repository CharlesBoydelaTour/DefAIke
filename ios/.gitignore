# Generated from project.yml by Scripts/generate-xcode-project.sh.
*.xcodeproj/
.xcodegen-cache/

# Local signing and bundle-identifier overrides. Created from
# Local.xcconfig.example by Scripts/generate-xcode-project.sh. Holds a developer's
# own Apple team identifier, which is personal and is not a release value.
Local.xcconfig

# SwiftPM and Xcode build products.
.build/
.swiftpm/
DerivedData/
*.xcuserdatad
xcuserdata/

# DefAIkePackage/Package.resolved is intentionally tracked, not ignored: the
# property-based dependency is exact-pinned and its resolved revision is part of
# the dependency reconciliation record.
